runtime: node
version: "20"
build:
  command: npm run build
start:
  command: node server.js
runtime_config:
  expose: 4000
env:
  NODE_ENV: production
  LOG_LEVEL: info
install:
  system_deps:
    - libssl-dev
    - curl
